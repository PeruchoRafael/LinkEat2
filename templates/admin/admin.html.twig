<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&family=Roboto:wght@100;300;400;500;700;900&display=swap"
          rel="stylesheet">
</head>
<body class="bg-white font-sans">

<div class="flex h-screen">

    <nav class="w-1/6 bg-gray-100 p-4">
        <div class="flex items-center mb-8">
            <img src="{{ asset('assets/logo.svg') }}" class="w-12 h-12 mr-4" alt="Logo">
            <h1 class="text-xl font-bold">LinkEat</h1>
        </div>
        <a href="#" class="bloc-link flex items-center mb-8">
            <img src="{{ asset('assets/dashboard.svg') }}" class="w-8 h-8 mr-4" alt="Dashboard">
            <span class="nav-links">Dashboard</span>
        </a>
        <a href="#" class="bloc-link flex items-center mb-8">
            <img src="{{ asset('assets/speech-bubble.svg') }}" class="w-8 h-8 mr-4" alt="Messages">
            <span class="nav-links">Messages</span>
        </a>
        <a href="#" class="bloc-link flex items-center active mb-8">
            <img src="{{ asset('assets/server.svg') }}" class="w-8 h-8 mr-4" alt="Base de données">
            <span class="nav-links">Base de donnée</span>
        </a>
        <a href="#" class="bloc-link flex items-center mb-8">
            <img src="{{ asset('assets/calendar.svg') }}" class="w-8 h-8 mr-4" alt="Calendrier">
            <span class="nav-links">Calendrier</span>
        </a>
    </nav>

    <main class="w-5/6 p-4">
        <div class="flex items-center mb-4">
            <label for="search">
                <img src="{{ asset('assets/search.svg') }}" class="w-6 h-6 mr-2" alt="Rechercher">
            </label>
            <input type="text" id="search" placeholder="Rechercher un utilisateur" class="w-full py-2 px-4 rounded-md border border-gray-300">
        </div>

        <h2 class="text-2xl mb-4">Résultats BDD</h2>

        <div class="grid grid-cols-3 gap-4">
            <div class="bg-gray-200 p-4">Nom</div>
            <div class="bg-gray-200 p-4">Email</div>
            <div class="bg-gray-200 p-4">Téléphone</div>
        </div>

        <div class="table-results">
            <!-- Results will be displayed here -->
        </div>
    </main>
</div>

<script src="{{ asset('js/script.js') }}"></script>
</body>
</html>
